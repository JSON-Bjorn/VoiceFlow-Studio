# VoiceFlow Studio - Unified Python Dependencies
# This file contains all Python dependencies for the entire project (backend + any Python tooling)
# 
# PYTHON VERSION REQUIREMENT: 3.11 (OPTIMAL FOR AI/TTS)
# - Chatterbox TTS requires Python >=3.9.0,<3.13
# - PyTorch 3.13 support is experimental only
# - Python 3.11 offers maximum AI/ML library compatibility

# =====================================================
# CORE FRAMEWORK & API
# =====================================================
fastapi==0.115.12
uvicorn==0.34.2
python-multipart==0.0.20
python-dotenv==1.1.0
requests==2.32.3

# Additional FastAPI dependencies
starlette==0.41.2

# =====================================================
# DATA & VALIDATION
# =====================================================
pydantic==2.11.5
pydantic-settings==2.9.1
email-validator==2.1.0
pandas>=2.0.0

# =====================================================
# DATABASE & ORM
# =====================================================
sqlalchemy==2.0.41
psycopg2-binary==2.9.10
alembic==1.16.1

# =====================================================
# AUTHENTICATION & SECURITY
# =====================================================
python-jose==3.4.0
passlib==1.7.4
bcrypt==4.3.0
python-jose[cryptography]==3.4.0

# =====================================================
# EXTERNAL APIS & PAYMENTS
# =====================================================
openai==1.82.0
stripe==12.1.0

# =====================================================
# GPU-ACCELERATED AI & TTS (PRODUCTION-READY)
# =====================================================
# PyTorch with CUDA support - ALIGNED WITH CHATTERBOX TTS
# Chatterbox TTS 0.1.1 requires exactly torch==2.6.0
# CUDA 12.1 wheels for optimal performance with your CUDA 12.6 installation
# Install command: pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121
torch==2.6.0
torchaudio==2.6.0

# Transformers for AI model support (compatible with PyTorch 2.6.0)
transformers>=4.46.0,<5.0.0

# *** CHATTERBOX TTS - THE ULTIMATE VOICE GENERATION ***
# Install: pip install chatterbox-tts==0.1.1
# Python 3.11 compatibility: PERFECT
chatterbox-tts==0.1.1

# PEFT for LoRA support (replaces deprecated LoRACompatibleLinear)
peft>=0.15.0,<1.0.0

# Diffusers with compatibility fixes for deprecation warnings
diffusers>=0.29.0,<1.0.0

# =====================================================
# AUDIO PROCESSING & MANIPULATION
# =====================================================
# PyDub for audio format conversion and processing
pydub>=0.25.1

# FFmpeg Python bindings for advanced audio operations
ffmpeg-python>=0.2.0

# Librosa for audio analysis (used in watermark detection)
librosa>=0.10.0

# SoundFile for audio I/O
soundfile>=0.12.1

# Accelerate for model optimization
accelerate>=0.25.0

# SafeTensors for secure model loading
safetensors>=0.4.0

# =====================================================
# OPTIONAL: WATERMARKING FOR RESPONSIBLE AI
# =====================================================
# Perth watermarking (uncomment to enable watermark detection)
# perth>=1.0.0

# =====================================================
# TESTING & DEVELOPMENT
# =====================================================
pytest==8.3.4
pytest-asyncio==0.25.0
pytest-cov==6.0.0
pytest-mock==3.14.0
httpx==0.28.1